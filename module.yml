api: "1.0"
name: "identity-provider"
type: node
script: server.js
hidden: true
config:
  - name: Passwords
    fields:
      - name: admin
        dispaly: Admin password
        type: password
        default: admin-pass
      - name: scorekeeper
        dispaly: Scorekeeper password
        type: password
        default: sk-pass
      - name: referee
        dispaly: Referee password
        type: password
        default: ref-pass
      - name: M.C.
        dispaly: M.C. password
        type: password
        default: mc-pass
